/**
 * bloc-ui-theme — CSS/SCSS design tokens only.
 *
 * Apply the default theme by adding to angular.json styles:
 *   "projects/bloc-ui-theme/styles/bloc-theme.scss"
 *
 * Or in your global styles.scss (after publishing to npm):
 *   @use 'bloc-ui-theme/styles/bloc-theme';
 *
 * Override any token in your own :root after importing:
 *   :root { --bloc-primary: #6366f1; }
 */

// ng-packagr requires at least one export.
export const BLOC_UI_THEME_VERSION = '0.0.1';
